The following are failing to build with soup3 patching:
slackbuilds/gnome-clocks

The gnome-all and gnome-basic queues will need to be revised to account for new dependencies.

SBo-builds-needing-upgrades/ are other maintainers projects, which all need to be coordinated with the rest of these upgrades.

